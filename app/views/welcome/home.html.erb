
<div class= 'welcome-golfer'>
  <p class="login-notice"><%= notice %></p>

  <h1>Welcome <%= current_golfer.name if current_golfer %></h1>
  <% if !current_golfer %> <h3 class="not-logged-in">Please Log in or Sign up </h3> <% end %>
</div>


<% if current_golfer %> <div class="home-info"></div><br> <% end %>

</container>

<script id="home-info-template" type="text/x-handlebars-template">
  <h4>Golfers in your Group: &nbsp;<span> {{groupSize}}</span></h4>
  <h4>Low Handicap Index: &nbsp; {{lowIndexGolfer.golfer_index}} for {{lowIndexGolfer.name}}</span></h4>
  <h4>Low Round: &nbsp;<span> {{lowRound.display_from_par}} with score of {{lowRound.score}} at {{lowRound.golf_course_name}} by {{lowRound.golfer_name}}</span></h4>
  <h4>Net Low Round: &nbsp;<span> Score of {{lowRoundNet.score}} and net {{lowRoundNet.display_net_from_par}} at {{lowRoundNet.golf_course_name}} by {{lowRoundNet.golfer_name}}</span></h4>
  <h4>Easiest Course: &nbsp;<span> {{lowSlopeCourse.name}} with course slope of {{lowSlopeCourse.course_slope}}</span></h4>
  <h4>Toughest Course: &nbsp;<span> {{highSlopeCourse.name}} with course slope of {{highSlopeCourse.course_slope}}</span></h4>
</script>
